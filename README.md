# jax_bhm_toymodel
JAX implementation of the BHM toy model
- Using a combination of JAX and JAX_MD to do integrative modeling within IMP to make use of GPUs

# To install this as a python package run : pip install -e .
